--- a/src/stylesheet/meson.build
+++ b/src/stylesheet/meson.build
@@ -3,7 +3,11 @@
 stylesheet_deps = []
 
 # For git checkouts, but not for tarballs...
-if not fs.exists('base.css')
+# The distributed tarball ships the pre-generated gtk.css (built from gtk.scss);
+# upstream tested for 'base.css', a file that never exists, so tarball builds
+# wrongly took the sass path and pulled in the sassc/libsass subprojects
+# (producing an unwanted libsass DLL).  Test for the actual generated file.
+if not fs.exists('gtk.css')
   sassc = find_program('sassc', required: false)
   if not sassc.found()
     subproject('sassc', default_options: ['warning_level=0', 'werror=false'])
